apiVersion: 1
providers:
  - name: REFORMMED Monitor
    orgId: 1
    folder: REFORMMED
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
